# Local DeepAgents Skills

This directory stores local development skills loaded through the DeepAgents `skills` configuration.

First-version default:

- [ ] Use local filesystem skills only.
- [ ] Configure `SKILLS_PATHS=./skills/examples/` for local smoke tests.
- [ ] Keep production or team-specific skills outside source control until reviewed.

